$(function() {
    var usernameEl = $('#username');
    var passwordEl = $('#password');

    /*
    if (!usernameEl.val() || 'admin_test' === usernameEl.val()) {
        usernameEl.val('admin_test');
        passwordEl.val('admin1');
    }
    */

});

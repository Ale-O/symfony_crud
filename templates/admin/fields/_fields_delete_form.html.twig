{{ include('admin/fields/_delete_fields_confirmation.html.twig') }}
<form action="{{ url('admin_textfields_delete', {id: textfields.id}) }}" method="post" data-confirmation="true" id="delete-form">
    <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
    <button type="submit" class="btn btn-lg btn-block btn-danger">
        <i class="fa fa-trash" aria-hidden="true"></i>
        {{ 'action.delete'|trans }}
    </button>
</form>

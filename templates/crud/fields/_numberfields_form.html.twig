{{ form_start(form, {id:"signaletique", method: 'POST', action: path('numberfields_edit', {'id': numberfields.id})}) }}
    <fieldset id={{ numberfields.id }}>

        {{ form_errors(form) }}
        {{ form_row(form.content, {'attr': {'onblur': 'this.form.submit()'} }) }}

    </fieldset>
    <br>
{{ form_end(form) }}
